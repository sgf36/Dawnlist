"""Turning one posting into the things you send: gaps, a CV, a letter, a brief.

Everything here runs on the USER'S own Anthropic key, or on no key at all, so
the marginal cost to Dawnlist of any of it is zero. That is the whole reason
this package can exist while a competitor charges separately for each piece.

The factsheet discipline from `app.outreach` applies unchanged: a claim the
evidence does not support is written as a visible placeholder and blocks the
export. It is imported rather than re-implemented — a second copy of that guard
would drift, and the direction it drifts in is the one that ships an invented
career detail.
"""
